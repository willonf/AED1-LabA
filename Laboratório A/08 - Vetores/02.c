#include "stdio.h"

void main() {
    int array[100];
    int n;
    float media = 0;
    int cont = 0;

    scanf("%d", &n);
    for (int i = 0; i < n; i++) {
        scanf("%d", &array[i]);
        cont++;
        // media /= con
    }
    
}